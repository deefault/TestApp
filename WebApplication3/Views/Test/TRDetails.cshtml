@using WebApplication3.Models;
@model TestResult

@{
    ViewBag.Title = "Результаты";
    Layout = "_Layout";
    //TODO: поправить дизайн
}
    <div class="container">
        <h4 class="text-center" style="padding-top:1rem">@ViewBag.Title</h4>
        <hr />
        <p>Название теста: @Model.Test.Name</p>
        <p>Вопросов: @Model.TotalQuestions</p>
        @if (Model.IsCompleted)
        {
            <p>Процент выполнения: @(Model.RightAnswersCount * 100 / Model.TotalQuestions)%</p>
            <a class="btn btn-primary" role="button"
               href="@Url.Action("AnswerResults", "Answer", new { testResultId = Model.Id, answerId = ViewBag.AnswerId })">
                Просмотреть
            </a>
        }
    </div>
